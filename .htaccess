<IfModule mod_rewrite.c>
    Options -Indexes
    RewriteEngine On

    # Send all requests into Laravel's public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

<IfModule !mod_rewrite.c>
    Options -Indexes
</IfModule>
